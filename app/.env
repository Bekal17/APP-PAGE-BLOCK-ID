# -----------------------------------------------------------------------------
# BlockID React frontend – environment variables (Vite)
# -----------------------------------------------------------------------------
# Variables prefixed with VITE_ are exposed to the client (import.meta.env).
# Do not put secrets here; they would be visible in the built bundle.
# -----------------------------------------------------------------------------

# AI Trust Backend API base URL (no trailing slash).
# The app sends POST requests to {VITE_TRUST_API_URL}/trust-score with { wallet }.
# Default in code: http://localhost:8000 (used when this is unset).
VITE_TRUST_API_URL=https://blockid-backend-production.up.railway.app
